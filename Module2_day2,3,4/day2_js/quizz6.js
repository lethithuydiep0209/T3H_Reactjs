// Hãy nhập vào 1 chuỗi s
// và 1 số l.
// Hãy thêm vào cuối chuỗi s ký tự ‘a’ cho đến khi độ dài chuỗi s >= l.

let s = prompt("Nhập chuỗi s:");
let l = parseInt(prompt("Nhập độ dài tối thiểu l:"));
while (s2.length < l) {
  s += 'a';
}
console.log("Chuỗi sau khi thêm:", s);
